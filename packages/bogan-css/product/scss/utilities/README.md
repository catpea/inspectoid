# Utilities

Small helpers for the component workshop shell:

- `.cf-scrollbar` applies a quiet scrollbar style.
- `.cf-icon-button` keeps icon-only controls stable.
- `.cf-fill` helps nested grid/flex children avoid overflow.
